import React from "react";

const Hero = () => {
  return (
    <div
      id="video-frame"
      className="relative z-10 h-dvh w-screen overflow-hidden rounded-lg bg-blue-75"
    >
      hero
    </div>
  );
};

export default Hero;
